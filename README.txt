1. About source code of voice, please look into the “client->voicecall" directory, and reference the "README" file in the “client->:voicecall" directory.

2. Follow the steps of “README” in “client->voicecall" directory, when completed, an executable file named "voice" will appear in the “client” directory. 

3. In "server" and "client" directories, make executable files "Server.x" and "Client.x" respectively.

4. Run "./Server.x user_info_file configuration_file" on server machine, run "./Client.x configuration_file" on client machine, make sure the "voice" executablefile is in the "client" directory.
I